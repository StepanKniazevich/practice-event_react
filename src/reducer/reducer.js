import {DELETE_LINK, ADD_LINK} from './boilerplate.js'

export default function reducer(state, action) {
    switch (action.type) {
        case ADD_LINK:
            {
                return {...state}
            }
        /* case DELETE_LINK:
            {
                return {...state}
            } */
            /* default:
                return {...state}; */
    }
}