
/* export const MenuFormValidate = ({error}) => {
    const [message, setMessage] = useState('Error , ');
    switch (error) {
        case NOT_UNIQUE:
            {
                setMessage((message) => message + 'this link already exists');
            }
        default:
            {}
    }

    return (<div className={styles.MenuFormError}>{message}</div>);
} */